<Window x:Class="PitWall.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
        xmlns:converters="clr-namespace:PitWall.Converters"
        xmlns:local="clr-namespace:PitWall"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <converters:SvgPathConverter x:Key="SvgPathConverter" />
    </Window.Resources>
    <Grid ShowGridLines="False">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="13*"/>
        </Grid.ColumnDefinitions>

        <ListBox ItemsSource="{Binding Drivers}" 
                 SelectedItem="{Binding SelectedDriver}" 
                 d:ItemsSource="{d:SampleData ItemCount=22}"
                 HorizontalContentAlignment="Stretch"
                 Background="Black">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid ShowGridLines="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="4*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock 
                            Grid.Column="0"
                            Text="{Binding Position}"
                            Foreground="White"
                            d:Text="1" />
                        <svg:SvgViewbox 
                            Grid.Column="1"
                            Source="{Binding TeamLogo, Converter={StaticResource SvgPathConverter}}"
                            Width="24"
                            Height="24"
                            />

                        <TextBlock
                            Grid.Column="2"
                            Text="{Binding ShortName}"
                            d:Text="SAI"
                            Foreground="White"/>


                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>



        <StackPanel Margin="20,20,20,20" Grid.Column="1">
            <TextBlock Text="Selected Driver:" FontWeight="Bold" />
            <TextBlock Text="{Binding SelectedDriver.FullName}" />
            <TextBlock Text="{Binding SelectedDriver.Position}" />
            <Button x:Name="Move_Up" Height="90" Width="160" HorizontalAlignment="Left" Margin="20,0,0,0" Content="Increase Position" Command="{Binding IncreasePositionCommand}"/>
            <Button x:Name="Move_Down" Height="90" Width="160" HorizontalAlignment="Left" Margin="20,0,0,0" Content="Decrease Position" Command="{Binding DecreasePositionCommand}"/>
        </StackPanel>
    </Grid>
</Window>
